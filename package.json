{
  "name": "remix-swa",
  "version": "0.0.1-alpha.0",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "web",
    "api"
  ],
  "scripts": {
    "build": "npm run build --workspace=web",
    "start": "npm run start --workspace=web",
    "clean": "npx rimraf node_modules package-lock.json web/node_modules web/dist api/dist api/build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^5.0.1"
  },
  "engines" : { 
    "node" : ">=16.0.0 <17.0.0"
  }
}
